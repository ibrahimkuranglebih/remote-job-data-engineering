services:
  postgres:
    container_name: remote_job_database
    image: postgres:17
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - ./sql/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - remote_job-network

  airflow:
    container_name: remote_job_dag
    image: apache/airflow:3.0.0
    ports:
      - "8000:8080"
    environment:
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: ${AIRFLOW__DATABASE__SQL_ALCHEMY_CONN}
      POSTGRES_HOST: ${POSTGRES_HOST}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      PYTHONPATH: /opt/airflow/etl
    volumes:
      - ./dag:/opt/airflow/dags
      - ./etl:/opt/airflow/etl
      - /var/run/docker.sock:/var/run/docker.sock
    group_add:
      - 1001
    depends_on:
      - postgres
    networks:
      - remote_job-network
    command: >
      bash -c "airflow standalone"

networks:
  remote_job-network:
    driver: bridge
